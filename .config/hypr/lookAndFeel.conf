#####################
### LOOK AND FEEL ###
#####################

source = colors.conf

# Waybar blur
layerrule[blur-waybar] {
    match:namespace = waybar
    blur = true
    ignore_alpha = 0
}

# Rofi animations
layerrule = blur on, match:namespace rofi

# Rofi blur
# Might not work
layerrule = blur on, match:namespace rofi
layerrule = ignore_alpha 0.5, match:namespace rofi

general {
    gaps_in = 2
    gaps_out = 3

    border_size = 2

    col.active_border = $inverse_primary
    col.inactive_border = $inverse_on_surface

    resize_on_border = false

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.97
    inactive_opacity = 0.90

    shadow {
        enabled = false
    }

    blur {
        enabled = true
        size = 3
        passes = 2
        new_optimizations = true
        xray = false

        vibrancy = 0.1696
    }
}

animations {
    enabled = yes, please :)

    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1
    bezier = smoothWorkspace, 0.25, 0.1, 0.25, 1

    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     2.4,  smoothWorkspace, slide
    animation = workspacesOut, 1,     2.4,  smoothWorkspace, slide
    animation = zoomFactor,    1,     7,     quick
}

master {
    new_status = master
}

misc {
    focus_on_activate = true
    force_default_wallpaper = 0 # Gooner wallpapers are not welcome on this OS
    disable_hyprland_logo = true
}
